// This module is kept for future use if pm2 targets need programmatic API access
// from within the runner. Currently, pm2 targets are standalone scripts that
// use the pm2 API directly — the runner spawns them via child_process.fork().
