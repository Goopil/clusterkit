{
  "name": "benchmarks",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "node runner.mjs",
    "bench:docker": "docker compose run --build --rm benchmark",
    "smoke": "node runner.mjs --smoke",
    "build": "echo 'no build step — all .mjs'"
  },
  "dependencies": {
    "@goopil/clusterkit": "workspace:*",
    "autocannon": "^8.0.0",
    "express": "^5.2.1",
    "pm2": "^7.0.4",
    "throng": "^5.0.0"
  },
  "packageManager": "pnpm@10.30.1",
  "engines": {
    "node": ">=22.12.0"
  }
}
